import { StyleSheet } from "react-native";

export default StyleSheet.create({
  loader: {
    width: 20,
    height: 20,
    borderRadius: 20/2,
    borderWidth: 4,
    borderColor: 'white',
    borderTopColor: 'transparent'
  }
});
